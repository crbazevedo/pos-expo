# POS-Expo: Poset-Exponential Reweighting

A Python library for importance weighting via exponential tilting based on structural features (rarity, difficulty, diversity).

## Installation

```bash
pip install .
```

## Structure

- `src/pos_expo/`: Core library code
- `tests/`: Unit tests
- `examples/`: Usage examples

